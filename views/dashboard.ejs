<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css" />
    
    <title>Candidate Dashboard</title>
</head>
<body>
    <div class="profile-container">
        <h1>Hello, <%= user.username %>!</h1>
        <p>Username: <%= user.username %></p>
        <p>Email: <%= user.email %></p>
    
        <h2>Your Job Applications</h2>
    
        <% if (applications.length > 0) { %>
            <ul class="list-group">
                <% applications.forEach(application => { %>
                    <li class="list-group-item">
                        <h3><%= application.jobId.title %></h3>
                        <p>Location: <%= application.jobId.location %></p>
                        <p>Salary: <%= application.jobId.salary %></p>
                        <p>Category: <%= application.jobId.category %></p>
                        <p>Requirements: <%= application.jobId.requirements %></p>
                        <p>Applied On: <%= new Date(application.createdAt).toLocaleDateString() %></p>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p class="no-applications">You have not applied for any jobs yet.</p>
        <% } %>
    </div>
    </body>
</html>
