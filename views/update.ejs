<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Update Job Listing</title>
</head>
<body class="update-page">
    <div class="update-container">
        <h2>Update Job Listing</h2>
        <form method="POST" action="/listings/<%= listing._id %>?_method=PUT">
            <div class="form-group">
                <label for="title">Job Title:</label>
                <input type="text" id="title" name="listing[title]" class="form-control" value="<%= listing.title %>">
            </div>
            <div class="form-group">
                <label for="description">Description:</label>
                <textarea id="description" name="listing[description]" class="form-control"><%= listing.description %></textarea>
            </div>
            <div class="form-group">
                <label for="location">Location:</label>
                <input type="text" id="location" name="listing[location]" class="form-control" value="<%= listing.location %>">
            </div>
            <div class="form-group">
                <label for="salary">Salary:</label>
                <input type="text" id="salary" name="listing[salary]" class="form-control" value="<%= listing.salary %>">
            </div>
            <div class="form-group">
                <label for="category">Category:</label>
                <input type="text" id="category" name="listing[category]" class="form-control" value="<%= listing.category %>">
            </div>
            <div class="form-group">
                <label for="requirements">Requirements:</label>
                <textarea id="requirements" name="listing[requirements]" class="form-control"><%= listing.requirements %></textarea>
            </div>
            <button type="submit" class="btn-primary">Update Listing</button>
        </form>
    </div>
</body>
</html>
